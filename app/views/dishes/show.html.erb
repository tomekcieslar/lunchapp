Dish <%= @dish.name  %>
<%= link_to t('buttons.back'),
  delivery_place_menu_path(@dish.menu.delivery_place_id,
  @dish.menu_id) 
%>
<%= link_to 'Edit', edit_menu_dish_path(@dish.menu_id, @dish.id) %>
